improve-workspace/
excalidraw-diagram/references/.venv/
**/.venv/
**/.pytest_cache/
**/.DS_Store
**/__pycache__/
*.pyc
node_modules/
thoughts/
.system/
.claude/

# mutation / CRAP 的中間產物。一律綁在 repo root（開頭的斜線）—— 不綁的話 `.coverage`
# 這種名字會在任何一層生效，把別的地方已經在版控的同名檔一起藏起來。
/mutants/
/.mutmut-cache
/.mutmut-run.log
/.coverage
/.coverage.json
/.radon-cc.json

# validator eval 的每跑結果；記錄在 evals/VALIDATOR-RESULTS.md，json 是跑完的中間產物。
/skills/skill-evolution/improve/evals/validator-eval-results.json

# classifier 的執行紀錄；夾在版控中的 signals.jsonl / skill-graph.json 之間容易被誤 commit，每跑一次又長一點。
/skills/skill-evolution/improve/memory/classifier.log
