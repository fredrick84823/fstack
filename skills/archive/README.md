# Archive

不再用、但還留著參考的 skill。**這個資料夾不出現在 `.claude-plugin/plugin.json` 的 `skills` array**，所以裝了 plugin 也不會載入。

**Model-invoked** — the model can reach for these when the description matches.

- **[deep-module](./deep-module/SKILL.md)**: Apply the Deep Module principle — human designs the interface outline, AI fills the implementation
